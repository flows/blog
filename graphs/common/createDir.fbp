title: Mkdir if not exists
ns: common
name: createDir

MkDir(fs/mkdir), HasDir(fs/exists), Complete(utils/dummy)

CopyPath(data/string) # temp sollution

-> in CopyPath

CopyPath out -> path HasDir
HasDir no   -> :start MkDir
HasDir yes  ->  in Complete

# persist must work
CopyPath out -> path MkDir :complete -> in Complete

<- out Complete
